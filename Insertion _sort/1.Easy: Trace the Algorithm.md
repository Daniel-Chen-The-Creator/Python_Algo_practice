Exercise 1 — Easy: Trace the Algorithm

Use insertion sort to arrange the following list in ascending order (smallest to largest):

nums = [7, 3, 5, 2, 6]

Requirements

Write the complete list after each insertion pass (4 passes in total).

Identify the current value being inserted, called key, in each pass.

List the numbers that move one position to the right during each pass.

Hint: Start by treating the first element, [7], as the sorted portion.

Suggested answer filename: q1_trace.md