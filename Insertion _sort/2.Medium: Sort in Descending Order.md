Exercise 2 — Medium: Sort in Descending Order

Complete the function below using insertion sort to arrange a list in descending order (largest to smallest):

def insertion_sort_desc(nums):
    # Write your code here.
    pass

Example

nums = [4, 9, 4, 1, 7, 3]
insertion_sort_desc(nums)
print(nums)

Expected output:

[9, 7, 4, 4, 3, 1]

Requirements

Modify the original list in place; do not create a separate sorted list.

Handle duplicate values, an empty list, and a list containing one element.

Think about: How should the main comparison condition change compared with ascending insertion sort?

Suggested solution filename: q2_descending.py